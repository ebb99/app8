/*CREATE TABLE zeiten (
    id SERIAL PRIMARY KEY,
    zeit TIMESTAMP WITHOUT TIME ZONE
);

CREATE TABLE vereine (
    id SERIAL PRIMARY KEY,
    vereinsname TEXT NOT NULL
);

select * from vereine
*/
